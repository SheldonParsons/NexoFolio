# 数据库迁移

新库入口为`modules/access/adapter/migrations`，只创建身份、项目、权限、环境相关表。

`legacy/`保留重置前已经执行的SQL原文与版本，禁止改写checksum。对于已经包含旧业务迁移版本的数据库，管理CLI使用此冻结账本进行兼容校验。它不意味着旧业务接口或worker仍然存在，也不会清理历史数据。

将来每个模块拥有独立迁移；数据角色及表权限必须在模块接入时测试。新模块不复用旧表作为隐式跨模块通道。
