# 修复后真实使用数据复核

结论：组件/弹窗采样在真实页面上已产生改善，普通HTTP记录的格式和内部一致性正常。当前输入可用于后端测试；复杂控件的底层值和跨视图/异步关联仍需明确保留未知，不应宣称全部参数映射已经完成。

## 数据边界

原179条基线 `backups/recording-baseline-20260916/` 保持不变。

本次补充快照：`backups/recording-followup-20260916-passive/`，数据集 `9ab07494-76ba-4ca6-b2c2-c90d23ebc774`，创建于2026-09-16 11:17:41 +08:00。快照含累计388条记录、39个接口；按原基线固定的event_id集合排除原179条，得到新增209条：83 HTTP、70页面上下文、44交互、12局部快照。不是把累计数据都当成新版本成果。

原始资料无缺失，累计287个内容寻址文件均校验；旧/新dump与tar的manifest哈希均通过。快照后服务已恢复，readiness200。审核从补充快照恢复到一次性数据库，查询READ ONLY，不改主库或基线，不调用模型，也没有重放真实业务请求。

## HTTP与归属

- 83条HTTP全部处理完成，响应状态均complete/200；81份JSON、2份HTML。
- 16份请求体为完整JSON，67份无body；本轮没有新的unreadable请求体样例。
- 209条已保存载荷/规范化元信息全部通过现行record Schema；HTTP正文声明字节数与已存内容一致。
- 记录ID及同上下文event_seq无重复；source_page与冻结page_url未见矛盾；起止时间均有效。
- 新记录采集到接收延迟约48–2091ms。这是本次样本范围，不是性能保证。
- 9条HTTP带operation_id，7条能按当前同视图规则配对UI；另2条能找到同一操作的交互，但发生SPA view/page变化。不将其误报为丢交互，也不强行改成同一视图。

这些检查验证已收到数据的内部一致性；没有独立浏览器网络/DOM对照，因此不证明全站零漏采或任意操作都不存在语义误归属。

## 真实业务采样改善

12份新增快照包含4份登录页、5份仅按钮的业务/确认界面，以及3份带输入/组件的业务快照。不能把33个input直接称作33个业务字段（其中含勾选控件）。

| 页面/位置 | 本次实际采到 | 说明 |
| --- | --- | --- |
| 咨询新增页弹窗 | 2个input、2个button | 捕获到有标签的局部表单；本次输入为空 |
| 咨询新增页选择弹窗 | 33个input，32个有标签 | 包含输入/勾选状态，不能据此推断完整数据字典 |
| 管理端技术咨询页 | 26个控件：17 input、6 div、1 span、2 button；2项选中文字 | 已读到自定义组件及选择文字，较之前仅按钮明显改善 |

出现DIALOG_FORM_ONLY、DIALOG_LOCAL_ONLY、RELATED_CONTAINER_AMBIGUOUS、CUSTOM_CONTROL_VALUE_UNKNOWN等新策略标记，确认新版路径确实运行。歧义场景退回目标控件；只有按钮的确认弹窗不必然是漏采表单。

## 尚未形成的知识

部分自定义控件只能观察到显示文字，底层值为unknown；查询输入还包含空值/重复值。当前累计ui_field_label/enum_label_candidate仍为0。这表示后端尚未建立控件到参数的映射，不等于这批原始载荷传错，也不能用猜测把显示文字变成ID。

新增HTTP中的异步、页面加载及轮询可能没有operation_id，属于当前明确边界；不以关联率低本身判定错误。用户A/B操作可以中断、交错，不能通过相邻时序拼出业务流程。

本次原始输入基本可用，先保持插件版本，不清空、不要求重复整套操作。后端后续测试以新旧固定快照的隔离副本进行，分别验证结构差异判定、值碰撞误关联、unknown/空值/重复控件值及跨视图边界。不要把候选枚举或关系已经正确作为前提。

本地机器统计：`experiments/results/passive-capture-followup/{audit,details,plugin-input-quality}.json`。其中interfaces/facts等表级统计为累计量，events/http/ui字段统计为新event_id差集；报告已按这个口径说明。
