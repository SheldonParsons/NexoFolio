# 真实观测文档质量检查

本轮只读核对已有数据，未修改前端、接口定义、差异状态或原始样例。
脚本：scripts/audit_observed_documents.py。使用独立的JSON字段/类型核对方式，原始正文只在内存中处理，输出聚合结果；并非再次调用同一Rust提取函数来验证自身。

结果：

- 31份当前定义与其原始样例对照；31份JSON响应、7份JSON请求类型和字段匹配，其他24份请求没有正文。
- 77条观测全部completed且有文档关联；未发现漏关联或字段/类型提取不匹配。
- 初始定义仍是各环境当前定义，未被后续差异自动覆盖。
- 1处待处理差异关联了4条观测：原定义的数组元素为object，后续空数组只能得到unknown，并新增EMPTY_ARRAY_ITEM_TYPE_UNKNOWN限制。

这处差异属于证据不足/弱化，不能据此断定接口删除字段或改变类型。目前已正确保留原定义，但尚缺“实际结构变化”和“样例信息不足”的进一步区分，不能把全部差异都解释为真正接口变更。
建议下一步先补这类比较语义和回归样例，再接自动分类。裁决、自动改写当前定义仍不在本轮检查中执行。
