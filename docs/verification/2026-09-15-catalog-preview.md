# 手动候选目录实施与验证

实现创建快照、手动生成、结构检查、候选保存、任务查看与文本报告导出。模块接口和行为见 ../contracts/0006-catalog-preview.md；详细结果见同名 JSON。

- 36项常规 Rust 测试、6项显式数据库集成测试、2项 Python 合同测试通过。
- fmt、Clippy、架构依赖规则及三个合同包一致性检查通过。
- 模型协议由本地隔离 HTTP 测试服务验证，覆盖成功、错误响应、截断、未知字段、不合法目录、失败重试、租约与代次隔离；不宣称真实模型或业务分类质量通过。
- PostgreSQL 用独立 schema 测试创建快照后新接口不混入、子目录先于父目录仍可原子保存、rejected不写可用节点、旧执行不能完成新任务、未配置模型不会错误改变pending状态。
- 已部署本地Linux容器并执行迁移，API readiness及前端代理均200。升级前完整备份，保留旧镜像。前端代码未修改。
- 31份已有定义和77条观测的只读审计在升级前后相同，未改变待确认差异或正式接口内容。

真实项目“商用订单管理系统”的31个接口已创建快照：

任务：`1b1b43ec-eb9c-468f-b4d4-ef7a1ce23e4e`
候选版本：`4a9727d7-9bdc-4564-b755-6555a6ef4e26`
初次创建状态为 `pending`；接入模型后现为 `ready`，已生成候选目录。

已在实际容器验证 catalog-run 缺少模型配置时明确失败，catalog-show 可读取原快照且任务仍pending。之后已按用户提供的 baitong model 配置完成真实生成：31个接口、9个目录、全部归属，结构检查通过，详见 2026-09-15-catalog-preview-model.md。ready仅表示结构检查通过，业务分类质量仍待用户确认。本轮没有发布、裁决、自动触发或提交推送。
