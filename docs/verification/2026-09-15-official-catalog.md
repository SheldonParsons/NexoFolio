# 正式目录交付验证

后端实现正式目录、固定待分类、候选发布、版本回退和幂等/代次控制；前端由front任务实现入口、用户确认操作、历史选择与实时成员详情。

41项常规Rust测试、6项数据库集成测试通过；另完成1项真实观测回放。fmt、Clippy和合同生成检查通过。前端任务报告86项测试及生产构建通过。发布/回退的真实HTTP写入只在隔离数据库中执行，覆盖幂等、并发409、跨项目及撤权、SQL失败回滚、系统目录保护、新增接口进入待分类和历史回退。

本地容器已更新并执行迁移，升级前已备份。用已授权账号读取真实正式目录接口，确认当前version_id=null/generation=0，固定待分类locked=true，共31份现有历史接口，发布历史为空。没有代用户发布候选、没有清空原项目，也没有用隔离回放数据替换原项目。

访问 /projects/9568a877-eb48-42ee-acbc-f9217f052386/catalog；用户可在候选页显式发布，之后通过正式页历史回退。初始目录和旧候选均保留。

遵守用户禁用Ego Lite要求，本轮没有调用、恢复或安装它；未宣称浏览器实际点击发布/回退。真实后端读取与隔离HTTP写入已有验证。未提交或推送。
