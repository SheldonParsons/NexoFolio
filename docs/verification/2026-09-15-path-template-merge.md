# 路径模板与候选合并验收

默认规则已包含纯数字、UUID、32/64位十六进制完整路径段。新上传先按项目/环境/方法/模板识别，再比较结构；原始URL和重试hash不变。项目可关闭、保留数字或排除路径前缀。识别结果写入inbox，worker按当次结果处理；旧队列仍保留字面路径。

已有历史文档和原始观测不物理合并、不删除；旧候选不改写。模型通过候选merge_groups集中展示历史重复接口，组内方法/目录一致、模板覆盖成员、成员不重叠、全部原始assignment保留。结构差异不由模型覆盖。

- 后端40项常规测试、6项数据库集成测试通过；最终数字关闭边界补充后contracts8项测试及完整Clippy再次通过。
- 覆盖首次数字路径、不同数字重复忽略、原URL和回执保留、结构变化转交、模板文档/Path参数、项目排除规则、模型非法合并组拒绝。
- 三套合同更新并核验，前端任务报告29项测试与生产构建通过。
- 新真实任务 `19e5e47b-6b7f-4319-b895-01d02c0d65a1` 耗时95.56秒，结构检查通过：31个原始接口、1个12成员合并组、20个展示项、10个候选目录。
- 浏览器实际显示31→20，点击“表格配置查询”显示12→1；展开合并组、先后选择两个成员，详情路径分别对应被选成员。尚未发布。
- 最后补充检查时ego-browser命令/skill路径失效（安装指针发生变化），未进一步操作或修复用户浏览器安装，无法执行finish；以上已完成的页面点击有实际快照证据。未声称完成最后的成员修订号DOM核对。

实现规则与范围见 ../contracts/0007-path-identification.md。候选原文位于忽略目录 experiments/results/path-merge-20260915/，未提交或推送。
